import {pipe} from "rxjs";

export interface VendorIndexVM {
  vendorId: string;
  vendorName: string;
  phoneNumber: string;
  email: string;
  contactPerson: string;
  city: string;
  zipCode: string;
  address: string;
  additionalComment: string;

}

